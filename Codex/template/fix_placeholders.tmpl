#FIX
目标章节：{{CHAPTER_TITLE}}

请通读我已完成的该章节内容，逐条查找【占位符】并尝试补强，仅补强，非重写：
- 可扩大本地材料范围：优先使用 Materials packs（如有）；仅在 Materials packs 不足以支撑“证据与出处”时，才遍历 Base packs 下的材料补齐证据（若 Base packs 的 Location 为 (none) 则跳过）。
- 若本地材料仍不足，可通过 SEC EDGAR 联网检索公司披露文件；也可使用其它公开网页资料，但必须在“证据与出处”中给出可追溯 URL。
- 当 Materials packs 和 Base packs 都为 (none) 且已提供 ticker 时，必须以 ticker 主动检索 SEC EDGAR 作为最小输入条件，禁止输出“缺口清单/未满足最小输入条件”。
- 如需新增来源，必须在允许范围内检索/引用，并在本章“证据与出处”按统一格式增量追加。
- 若检索后仍无法补强，请在原占位符处追加固定句式：
【占位符：缺口】暂未在（已检索范围）中检索到公司披露/监管披露对该点的明确表述；以最终财报/监管披露为准。

输出要求：仅输出该章节正文（从“## {{CHAPTER_TITLE}}”到该章结束），不要输出其它解释文字。

公司名字：{{COMPANY}}
TICKER：{{TICKER}}

以下为本章正文：
{{CHAPTER_MARKDOWN}}

材料清单（供快速浏览；可能同时包含 Materials packs 与 Base packs 两部分）：
{{MATERIALS_MANIFEST}}
